# Slide assets

Replace these placeholder references before the live pitch:

Filenames must match **exactly** (no leading or trailing spaces). If `\IfFileExists` fails, LaTeX shows the gray placeholder box instead of the image.

- `smith.png`: Smith looking sheepish next to `.env`.
- `loop.png`: Optional exported loop diagram. The LaTeX deck has a TikZ fallback if this is missing.
- `wow-diff.png`: Screenshot of the live `AGENT_WIT.yaml` amendment diff.
- `qr.png`: QR code to the public repo or final one-link page.

Keep images in a 16:9-friendly shape where possible. For QR codes, use a large square PNG with enough quiet border to scan from the back of the room.
